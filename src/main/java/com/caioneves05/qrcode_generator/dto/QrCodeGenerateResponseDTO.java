package com.caioneves05.qrcode_generator.dto;

public record QrCodeGenerateResponseDTO(String url) {
    // This record can be extended with additional fields as needed for the QR code generation response.
    // For example, you might want to include a status message, error details, or other relevant information.
}
